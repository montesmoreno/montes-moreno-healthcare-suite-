import { json, db, requireRole, employeeById, clinicByName, centralDate, payrollDates, handlerError } from "./_clock.js";
export default async function handler(req,res){
  if(req.method!=="POST")return json(res,405,{error:"Method not allowed"});
  try{const user=requireRole(req,"employee"); const employee=await employeeById(user.employeeId); if(!employee||!employee.active||!employee.time_clock_enabled)return json(res,403,{error:"Employee is not authorized to use Time Clock."}); const clinic=await clinicByName(String(req.body?.workClinic||""),employee.organization_id); if(!clinic)return json(res,400,{error:"Please select a valid work clinic."}); const employeeNote=String(req.body?.employeeNote||"").trim().slice(0,1000); const workDate=centralDate(); const {data:existing,error:ee}=await db().from("time_records").select("id").eq("employee_record_id",employee.id).eq("work_date",workDate).maybeSingle(); if(ee)throw ee; if(existing)return json(res,409,{error:"A time record already exists for today."}); const period=payrollDates(workDate); const {data,error}=await db().from("time_records").insert({organization_id:employee.organization_id,employee_record_id:employee.id,clinic_id:clinic.id,employee_id:employee.employee_id,employee_name:employee.full_name,work_date:workDate,clock_in:new Date().toISOString(),hourly_rate_snapshot:employee.hourly_rate,status:"clocked_in",employee_note:employeeNote,pay_period_start:period.start,pay_period_end:period.end,pay_date:period.payDate,source_system:"supabase"}).select().single(); if(error)throw error; return json(res,200,{ok:true,message:"Clock-in completed successfully.",record:data});}catch(e){return handlerError(res,e);}
}

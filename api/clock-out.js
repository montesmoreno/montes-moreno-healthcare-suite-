import { json, db, requireRole, centralDate, hoursBetween, handlerError } from "./_clock.js";
export default async function handler(req,res){
  if(req.method!=="POST")return json(res,405,{error:"Method not allowed"});
  try{const user=requireRole(req,"employee"); const {data:record,error:le}=await db().from("time_records").select("*").eq("employee_record_id",user.sub).eq("work_date",centralDate()).is("clock_out",null).maybeSingle(); if(le)throw le; if(!record)return json(res,409,{error:"No active clock-in was found."}); const newNote=String(req.body?.employeeNote||"").trim().slice(0,1000); const employeeNote=[record.employee_note,newNote].filter(Boolean).join("\n").slice(0,2000); const out=new Date().toISOString(); const hours=hoursBetween(record.clock_in,out); const gross=Math.round(hours*Number(record.hourly_rate_snapshot)*100)/100; const {data,error}=await db().from("time_records").update({clock_out:out,hours_worked:hours,gross_pay:gross,status:"clocked_out",employee_note:employeeNote,updated_at:out}).eq("id",record.id).select().single(); if(error)throw error; return json(res,200,{ok:true,message:"Clock-out completed successfully.",hoursWorked:hours,grossPay:gross,record:data});}catch(e){return handlerError(res,e);}
}
